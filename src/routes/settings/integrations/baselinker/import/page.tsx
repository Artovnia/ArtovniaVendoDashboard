/**
 * BaseLinker Import Page
 * 
 * Simple wrapper component that renders the BaseLinkerImport modal.
 * All state management is handled within BaseLinkerImport component.
 */
import { BaseLinkerImport } from './baselinker-import'

export default function BaseLinkerImportPage() {
  return <BaseLinkerImport />
}
